# Network interface used to communicate (eth0 default for wired connections)
iface = "en0"

# N = how many sites to fingerprint, R = if the sites should be random, M = max index of site to fingerprint (given that the sites are random)
N = 5
R = False
M = 1000000